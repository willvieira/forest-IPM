% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{parameters}
\alias{parameters}
\title{Resolve a single parameter realization from Bayesian posteriors}
\usage{
parameters(mod, draw = "random", seed = NULL)
}
\arguments{
\item{mod}{An \code{ipm_spModel} object.}

\item{draw}{Character \code{"mean"} or \code{"random"}, or a positive integer
1-1000 selecting a specific posterior draw index.}

\item{seed}{Integer or NULL. Random seed. When \code{draw = "random"} and
\code{seed = NULL}, a seed is auto-generated so the draw is reproducible.
Retrieve it from \code{$seed} on the returned object.}
}
\value{
An object of S3 class \code{"ipm_parameters"}.
}
\description{
Resolve a single parameter realization from Bayesian posteriors
}
\examples{
df <- data.frame(size_mm = c(150, 200, 350),
                 species_id = "ABIBAL",
                 plot_size  = 1000)
s    <- stand(df)
mod  <- species_model(s)
pars <- parameters(mod, draw = "mean")
print(pars)
}
