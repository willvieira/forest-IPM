% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{set_random_effects}
\alias{set_random_effects}
\title{Set plot-level random effects for one or more species}
\usage{
set_random_effects(pars, values, species = NULL)
}
\arguments{
\item{pars}{An \code{ipm_parameters} object.}

\item{values}{A numeric vector of length 3 (growth, survival, recruitment
offsets), applied to all species named in \code{species}; \emph{or} a
named list keyed by species ID, each element a numeric vector of length 3.}

\item{species}{Character vector of species IDs. Used only when \code{values}
is a numeric vector; ignored when \code{values} is a named list.
If \code{NULL} and \code{values} is a numeric vector, applies to all species.}
}
\value{
The modified \code{ipm_parameters} object.
}
\description{
Populates the \code{$random_effects} slot inside each species entry of an
\code{ipm_parameters} object. Engines (\code{lambda()}, \code{project()})
default to \code{c(0, 0, 0)} for any species whose slot is \code{NULL}.
}
\examples{
df <- data.frame(size_mm = c(150, 200, 350),
                 species_id = "ABIBAL",
                 plot_size  = 1000)
s     <- stand(df)
mod   <- species_model(s)
pars  <- parameters(mod, draw = "mean")
pars2 <- set_random_effects(pars, values = c(0, 0, 0))
}
