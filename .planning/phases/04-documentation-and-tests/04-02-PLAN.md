---
phase: 04-documentation-and-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - R/stand.R
  - R/species_model.R
  - R/parameters.R
  - R/env_condition.R
  - R/control.R
  - R/lambda.R
  - R/project.R
  - R/supported_species.R
  - R/env_scaling.R
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "?stand opens a help page with @param, @return, and @examples sections"
    - "?lambda opens a help page with @param, @return, and @examples sections"
    - "devtools::run_examples() completes without errors or warnings"
    - "All 11 exported functions have runnable @examples that do not require network access"
  artifacts:
    - path: "R/stand.R"
      provides: "@examples block for stand()"
      contains: "@examples"
    - path: "R/lambda.R"
      provides: "@examples block for lambda()"
      contains: "@examples"
    - path: "R/parameters.R"
      provides: "@examples block for parameters() and set_random_effects()"
      contains: "@examples"
    - path: "R/env_scaling.R"
      provides: "@examples blocks for scale_env() and unscale_env()"
      contains: "@examples"
  key_links:
    - from: "R/*.R @examples blocks"
      to: "inst/extdata/parameters/ABIBAL_pars.rds"
      via: "parameters(mod, draw = 'mean') offline lookup"
      pattern: "draw = .mean."
    - from: "man/*.Rd files"
      to: "R/*.R @examples"
      via: "roxygen2::roxygenise(load_code = 'source')"
      pattern: "examples"
---

<objective>
Add @examples sections to all 11 exported functions so that devtools::run_examples() passes without errors and ?function_name opens a complete help page.

Purpose: DOC-01 requires all exported functions to have runnable @examples. This plan adds only the missing @examples — @param and @return already exist. All examples use ABIBAL with draw="mean" and are verified offline.

Output: 11 exported functions with @examples in their roxygen blocks; man/*.Rd files regenerated via roxygenise.
</objective>

<execution_context>
@/Users/wvieira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wvieira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-documentation-and-tests/04-CONTEXT.md
@.planning/phases/04-documentation-and-tests/04-RESEARCH.md
@.planning/phases/04-documentation-and-tests/04-01-SUMMARY.md

<interfaces>
<!-- Standard offline example block — reused across all 11 functions. -->

Canonical minimal stand (works offline, no network):
```r
df <- data.frame(size_mm = c(150, 200, 350),
                 species_id = "ABIBAL",
                 plot_size  = 1000)
s    <- stand(df)
mod  <- species_model(s)
pars <- parameters(mod, draw = "mean")
env  <- env_condition(MAT = 8, MAP = 1200)
ctrl <- control(years = 5, compute_lambda = TRUE, progress = FALSE)
```

Exported functions needing @examples (from NAMESPACE):
1. stand()           — R/stand.R
2. species_model()   — R/species_model.R
3. parameters()      — R/parameters.R
4. set_random_effects() — R/parameters.R
5. env_condition()   — R/env_condition.R
6. control()         — R/control.R
7. lambda()          — R/lambda.R
8. project()         — R/project.R  (plot.ipm_projection @examples added in Plan 01)
9. supported_species() — R/supported_species.R
10. scale_env()      — R/env_scaling.R
11. unscale_env()    — R/env_scaling.R

Pitfalls (from RESEARCH.md):
- Use roxygen2::roxygenise(load_code='source') NOT devtools::document() — avoids Xcode C++ compilation (blocker on this machine)
- Use control(years = 5, progress = FALSE) in all examples — never years > 10
- All examples must use ABIBAL only — confirmed present in inst/extdata/parameters/
- Do NOT call devtools::run_examples() from within Rscript -e (it calls devtools::load_all which triggers Rcpp compilation) — run via interactive R session or use roxygen2::roxygenise + check .Rd files instead
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @examples to all 11 exported functions</name>
  <files>
    R/stand.R,
    R/species_model.R,
    R/parameters.R,
    R/env_condition.R,
    R/control.R,
    R/lambda.R,
    R/project.R,
    R/supported_species.R,
    R/env_scaling.R
  </files>
  <action>
Read each file first to locate the existing roxygen block above each exported function. Insert @examples sections after the @return tag (and before any @export tag if one appears below @return). Do not remove or modify existing @param or @return lines.

Use the canonical minimal stand pattern for all examples that need to set up a workflow. Keep examples short — the goal is runnable demonstration, not exhaustive coverage.

--- R/stand.R: stand() ---
Add after @return:
```r
#' @examples
#' df <- data.frame(size_mm = c(150, 200, 350),
#'                  species_id = "ABIBAL",
#'                  plot_size  = 1000)
#' s <- stand(df)
#' print(s)
```

--- R/species_model.R: species_model() ---
Add after @return:
```r
#' @examples
#' df <- data.frame(size_mm = c(150, 200, 350),
#'                  species_id = "ABIBAL",
#'                  plot_size  = 1000)
#' s   <- stand(df)
#' mod <- species_model(s)
#' print(mod)
```

--- R/parameters.R: parameters() ---
Add after @return:
```r
#' @examples
#' df <- data.frame(size_mm = c(150, 200, 350),
#'                  species_id = "ABIBAL",
#'                  plot_size  = 1000)
#' s    <- stand(df)
#' mod  <- species_model(s)
#' pars <- parameters(mod, draw = "mean")
#' print(pars)
```

--- R/parameters.R: set_random_effects() ---
Add after @return:
```r
#' @examples
#' df <- data.frame(size_mm = c(150, 200, 350),
#'                  species_id = "ABIBAL",
#'                  plot_size  = 1000)
#' s    <- stand(df)
#' mod  <- species_model(s)
#' pars <- parameters(mod, draw = "mean")
#' pars2 <- set_random_effects(pars, plot_random = c(0, 0, 0))
```

--- R/env_condition.R: env_condition() ---
Add after @return:
```r
#' @examples
#' env <- env_condition(MAT = 8, MAP = 1200)
#' print(env)
```

--- R/control.R: control() ---
Add after @return:
```r
#' @examples
#' ctrl <- control(years = 10, compute_lambda = TRUE, progress = FALSE)
#' print(ctrl)
```

--- R/lambda.R: lambda() ---
Add after @return:
```r
#' @examples
#' df <- data.frame(size_mm = c(150, 200, 350),
#'                  species_id = "ABIBAL",
#'                  plot_size  = 1000)
#' s    <- stand(df)
#' mod  <- species_model(s)
#' pars <- parameters(mod, draw = "mean")
#' env  <- env_condition(MAT = 8, MAP = 1200)
#' ctrl <- control(years = 5, compute_lambda = TRUE, progress = FALSE)
#' lam  <- lambda(mod, pars, s, env, ctrl)
#' print(lam)
```

--- R/project.R: project() ---
Add after @return (note: plot.ipm_projection @examples already added in Plan 01):
```r
#' @examples
#' df <- data.frame(size_mm = c(150, 200, 350),
#'                  species_id = "ABIBAL",
#'                  plot_size  = 1000)
#' s    <- stand(df)
#' mod  <- species_model(s)
#' pars <- parameters(mod, draw = "mean")
#' env  <- env_condition(MAT = 8, MAP = 1200)
#' ctrl <- control(years = 5, compute_lambda = TRUE, progress = FALSE)
#' proj <- project(mod, pars, s, env, ctrl)
#' print(proj)
```

--- R/supported_species.R: supported_species() ---
Add after @return:
```r
#' @examples
#' sp <- supported_species()
#' head(sp)
```

--- R/env_scaling.R: scale_env() ---
Add after @return:
```r
#' @examples
#' scaled <- scale_env(MAT = 8, MAP = 1200)
```

--- R/env_scaling.R: unscale_env() ---
Add after @return:
```r
#' @examples
#' scaled <- scale_env(MAT = 8, MAP = 1200)
#' original <- unscale_env(scaled)
```

After adding all @examples sections, regenerate man/*.Rd files:
```r
roxygen2::roxygenise(load_code = "source")
```
(Per Phase 02-03 decision — use roxygenise with load_code='source' NOT devtools::document() to avoid C++ compilation / Xcode license blocker.)

Verify that man/ now contains .Rd files with \examples{} sections for each exported function.
  </action>
  <verify>
    <automated>cd /Users/wvieira/GitHub/forest-IPM && Rscript -e "roxygen2::roxygenise(load_code='source', quiet=TRUE); rds <- readLines('man/stand.Rd'); if(any(grepl('examples', rds))) cat('PASS: stand.Rd has examples\n') else stop('FAIL: stand.Rd missing examples'); rds2 <- readLines('man/lambda.Rd'); if(any(grepl('examples', rds2))) cat('PASS: lambda.Rd has examples\n') else stop('FAIL: lambda.Rd missing examples')"</automated>
  </verify>
  <done>All 11 exported functions have @examples in their roxygen block. man/*.Rd files contain \examples{} sections. roxygen2::roxygenise(load_code='source') runs without errors. NAMESPACE is unchanged (same exports as before).</done>
</task>

<task type="auto">
  <name>Task 2: Verify examples run offline and measure coverage</name>
  <files></files>
  <action>
This is a verification-only task — no file modifications.

Step 1 — Verify examples load and run (offline smoke test):
Run a minimal inline verification that exercises the example patterns:

```r
devtools::load_all(quiet = TRUE)

# Test canonical example pattern runs without error
df <- data.frame(size_mm = c(150, 200, 350),
                 species_id = "ABIBAL", plot_size = 400)
s    <- stand(df)
mod  <- species_model(s)
pars <- parameters(mod, draw = "mean")
env  <- env_condition(MAT = 8, MAP = 1200)
ctrl <- control(years = 5, compute_lambda = TRUE, progress = FALSE)
lam  <- lambda(mod, pars, s, env, ctrl)
proj <- project(mod, pars, s, env, ctrl)
sp   <- supported_species()
cat("All canonical examples run offline: PASS\n")

# scale_env / unscale_env pattern
scaled <- scale_env(MAT = 8, MAP = 1200)
cat("scale_env / unscale_env: PASS\n")
```

Step 2 — Measure line coverage on the 6 core target files:
```r
cov <- covr::package_coverage(path = ".", type = "tests", quiet = TRUE)
pct <- covr::percent_coverage(cov)
cat(sprintf("Total coverage: %.1f%%\n", pct))

# Check individual target files
target_files <- c("R/stand.R", "R/species_model.R", "R/parameters.R",
                  "R/kernel.R", "R/lambda.R", "R/project.R")
for (f in target_files) {
  fc <- covr::file_coverage(cov, f)
  cat(sprintf("%s: %.1f%%\n", f, covr::percent_coverage(fc)))
}
```

If any target file is below 80%, note which lines are uncovered and report in the SUMMARY. The 80% target is per-file on these 6 files. If total coverage is below 60%, that indicates missing test breadth — add additional test_that() blocks to test-workflow.R to close gaps.

Step 3 — Run full test suite:
```r
devtools::test()
```
Confirm zero failures.
  </action>
  <verify>
    <automated>cd /Users/wvieira/GitHub/forest-IPM && Rscript -e "devtools::load_all(quiet=TRUE); df <- data.frame(size_mm=c(150,200,350), species_id='ABIBAL', plot_size=1000); s <- stand(df); mod <- species_model(s); pars <- parameters(mod, draw='mean'); env <- env_condition(MAT=8, MAP=1200); ctrl <- control(years=5, compute_lambda=TRUE, progress=FALSE); proj <- project(mod, pars, s, env, ctrl); cat('PASS: full workflow offline\n')"</automated>
  </verify>
  <done>Full workflow runs offline without errors. devtools::test() reports zero failures. Coverage report produced and recorded in SUMMARY — note which target files are above/below 80%.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify end-to-end:

1. man/ directory contains .Rd files with \examples{} for all 11 exports:
   ```
   grep -l "examples" man/*.Rd | wc -l   # should be >= 11
   ```

2. NAMESPACE unchanged (same 11 exports + 16 S3methods as before):
   ```
   cat NAMESPACE
   ```

3. devtools::test() zero failures.

4. Coverage >= 60% overall; target files noted in SUMMARY.
</verification>

<success_criteria>
- All 11 exported functions have @examples in their roxygen blocks
- man/*.Rd files contain \examples{} sections (verified by grepping)
- roxygen2::roxygenise(load_code='source') completes without errors
- The offline example pattern (ABIBAL, draw="mean", years=5) runs without network access
- devtools::test() zero failures
- Coverage report produced and recorded in SUMMARY (even if below 80% — gap closure is a separate task)
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-and-tests/04-02-SUMMARY.md`
</output>
