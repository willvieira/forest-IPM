---
phase: 02-package-skeleton-and-bug-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - DESCRIPTION
  - NAMESPACE
  - R/globals.R
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
  - PKG-05

must_haves:
  truths:
    - "devtools::install_github('wvieira/forest-IPM') succeeds on a fresh R environment"
    - "R CMD check produces zero errors and zero warnings (NOTEs about missing documentation are acceptable)"
    - "devtools::load_all() compiles the Rcpp C++ eigenvalue solver without errors"
    - "mkKernel() and getEigenValues() are accessible after load_all()"
  artifacts:
    - path: "DESCRIPTION"
      provides: "Package metadata with all Imports and LinkingTo declarations"
      contains: "forestIPM"
    - path: "NAMESPACE"
      provides: "Generated by roxygen2; includes useDynLib and all importFrom declarations"
      contains: "useDynLib(forestIPM"
    - path: "R/globals.R"
      provides: "Centralized @useDynLib and @importFrom roxygen tags"
      contains: "@importFrom Rcpp sourceCpp"
  key_links:
    - from: "R/globals.R"
      to: "NAMESPACE"
      via: "devtools::document() processes @useDynLib and @importFrom tags into NAMESPACE"
      pattern: "useDynLib\\(forestIPM"
    - from: "DESCRIPTION LinkingTo"
      to: "src/eigen.cpp compilation"
      via: "Rcpp and RcppEigen headers resolved during devtools::load_all() C++ compilation"
      pattern: "LinkingTo:"
    - from: "DESCRIPTION Imports"
      to: "R/params.R and R/kernel.R runtime symbols"
      via: "Package search path makes all Imports available at runtime without ::"
      pattern: "Imports:"
---

<objective>
Create the R package infrastructure that wraps the existing source code into an installable, checkable R package. This plan produces DESCRIPTION, NAMESPACE, and R/globals.R — the three files that make `devtools::install_github()` and `R CMD check` succeed.

Purpose: Without DESCRIPTION the repo is not an R package. Without a correct NAMESPACE the C++ shared library does not load and namespace imports are missing, causing `R CMD check` errors. This plan turns a collection of R scripts into a distributable package.

Output:
- `DESCRIPTION` — package metadata, all Imports with version constraints, LinkingTo for Rcpp/RcppEigen
- `NAMESPACE` — generated by roxygen2 via `devtools::document()`; includes useDynLib, importFrom declarations
- `R/globals.R` — centralized roxygen2 tags for useDynLib and all importFrom symbols
</objective>

<execution_context>
@/Users/wvieira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wvieira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-skeleton-and-bug-fixes/02-RESEARCH.md
@.planning/phases/02-package-skeleton-and-bug-fixes/02-01-SUMMARY.md

<interfaces>
<!-- Symbols used in existing R files that must be covered by @importFrom in globals.R. -->
<!-- Inventoried from R/params.R and R/kernel.R source inspection. -->

From R/params.R — symbols requiring @importFrom:
- purrr: map, map_chr
- dplyr: filter, group_by, reframe, select, mutate
- tidyr: pivot_wider, pivot_longer
- stringr: str_replace
- rlang: .data, set_names
- magrittr: %>%
  (Note: case_match is from dplyr — add to dplyr importFrom list)

From R/kernel.R — used via ::, no @importFrom needed:
- truncnorm::dtruncnorm  (needs truncnorm in DESCRIPTION Imports only)

From src/eigen.cpp — needs C++ compilation hooks in NAMESPACE:
- useDynLib(forestIPM, .registration = TRUE)
- importFrom(Rcpp, sourceCpp)

Column names used bare in dplyr verbs in params.R (need .data pronoun):
- par, value, iter, name, vr  (in filter(), group_by(), reframe(), select(), mutate())
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DESCRIPTION file with all package metadata and dependencies</name>
  <files>DESCRIPTION</files>
  <action>
Create DESCRIPTION in the package root (/Users/wvieira/GitHub/forest-IPM/DESCRIPTION) with the following exact content:

```
Package: forestIPM
Type: Package
Title: Bayesian Hierarchical Integral Projection Model for Tree Population Dynamics
Version: 0.1.0
Authors@R:
    person("Willian", "Vieira", , "willian.vieira@usherbrooke.ca", role = c("aut", "cre"))
Description: Integral Projection Model for studying tree population dynamics
    (population growth rates, coexistence, sensitivity) in eastern North America.
    Uses Bayesian hierarchical vital rate models parameterized from forest inventory data.
License: MIT + file LICENSE
Encoding: UTF-8
Roxygen: list(markdown = TRUE)
RoxygenNote: 7.3.2
Depends:
    R (>= 4.1.0)
Imports:
    Rcpp (>= 1.0.12),
    dplyr (>= 1.1.4),
    tidyr (>= 1.3.1),
    purrr (>= 1.1.2),
    stringr (>= 1.5.1),
    rlang (>= 1.1.4),
    magrittr (>= 2.0.3),
    truncnorm (>= 1.0-9)
LinkingTo:
    Rcpp,
    RcppEigen
```

Notes on specific fields:
- `Depends: R (>= 4.1.0)` — required because params.R uses native pipe `|>` mixed with `%>%`; R 4.1.0 introduced native pipe
- `truncnorm` is in Imports (not Suggests) because kernel.R calls `truncnorm::dtruncnorm()` at runtime with `::` notation — runtime dependency requires Imports
- `magrittr` stays in Imports because params.R and pars_to_list() use `%>%` (migrating to native `|>` is deferred)
- `RcppEigen` in LinkingTo only (not Imports) — provides C++ headers, not R-level functions
- Do NOT add `RcppEigen` to Imports — this is a common mistake; it's header-only from R's perspective
  </action>
  <verify>
    <automated>cd /Users/wvieira/GitHub/forest-IPM && Rscript -e "desc <- read.dcf('DESCRIPTION'); cat('Package:', desc[,'Package'], '\n'); cat('Rcpp in Imports:', grepl('Rcpp', desc[,'Imports']), '\n'); cat('RcppEigen in LinkingTo:', grepl('RcppEigen', desc[,'LinkingTo']), '\n')"</automated>
  </verify>
  <done>DESCRIPTION file exists and is valid DCF format. Package name is forestIPM. Rcpp appears in Imports. RcppEigen appears in LinkingTo. R CMD check can parse it without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create R/globals.R and generate NAMESPACE via devtools::document()</name>
  <files>R/globals.R, NAMESPACE</files>
  <action>
Step A — Create R/globals.R with all centralized roxygen2 import declarations:

```r
# globals.R
# Centralized package-level roxygen2 declarations:
# - Rcpp shared library registration (required for getEigenValues() to load)
# - All @importFrom declarations for symbols used in params.R without ::

#' @useDynLib forestIPM, .registration = TRUE
#' @importFrom Rcpp sourceCpp
#' @importFrom purrr map map_chr
#' @importFrom dplyr filter group_by reframe select mutate case_match
#' @importFrom tidyr pivot_wider pivot_longer
#' @importFrom stringr str_replace
#' @importFrom rlang .data set_names
#' @importFrom magrittr %>%
NULL
```

Step B — Run devtools::document() to generate NAMESPACE:

```r
devtools::document()
```

This regenerates NAMESPACE from all `@importFrom` and `@useDynLib` tags across all R files (globals.R and any other files with `@export`). Do NOT write NAMESPACE by hand — roxygen2 owns it.

Step C — Verify NAMESPACE contains the required lines. After document() runs, NAMESPACE must contain:
- `useDynLib(forestIPM, .registration = TRUE)`
- `importFrom(Rcpp,sourceCpp)`
- `importFrom(purrr,map)`
- `importFrom(dplyr,filter)`
- `exportPattern` or explicit `export()` lines for mkKernel, init_pop, getPars_sp (from their `@export` tags already in source)

Step D — Address "no visible binding" warnings in params.R by adding `.data$` prefix to bare column references:

In R/params.R, update dplyr/tidyr verb calls to use `.data$colname` instead of bare `colname`:

In getPars_sp() method == 'mean' pipe (around lines 40-44):
```r
filter(.data$par != 'lp__') |>
  group_by(.data$par) |>
  reframe(value = mean(.data$value)) |>
  pivot_wider(names_from = 'par') |>
  unlist(use.names = TRUE)
```

In getPars_sp() method == 'random' pipe (around lines 49-54):
```r
filter(.data$par != 'lp__') |>
  filter(.data$iter == sample(1:4000, 1)) |>
  select(!.data$iter) |>
  pivot_wider(names_from = 'par') |>
  unlist(use.names = TRUE)
```

In pars_to_list() (around lines 65-87):
```r
mutate(
  vr = str_replace(.data$name, '\\..*', ''),
  par = str_replace(.data$name, paste0(.data$vr, '.'), ''),
  vr = case_match(
    .data$vr,
    'recruit' ~ 'rec',
    .default = .data$vr
  )
) |>
select(!.data$name) |>
```

Note: `names_from = 'par'` in pivot_wider uses a quoted string — already correct, no .data needed there.
Note: `select(contains('.'))` uses a tidyselect helper — already correct, no .data needed.

After all edits, run devtools::document() again to ensure NAMESPACE is up to date.
  </action>
  <verify>
    <automated>cd /Users/wvieira/GitHub/forest-IPM && Rscript -e "devtools::document(); cat('document() done\n')" && grep -c "useDynLib" NAMESPACE && grep -c "importFrom" NAMESPACE</automated>
  </verify>
  <done>R/globals.R exists with all @importFrom declarations. NAMESPACE was generated by devtools::document() and contains at least 1 useDynLib line and multiple importFrom lines. R/params.R uses .data$ pronoun on all bare column references.</done>
</task>

<task type="auto">
  <name>Task 3: Run R CMD check and resolve all errors and warnings</name>
  <files>DESCRIPTION, NAMESPACE, R/globals.R</files>
  <action>
Run devtools::check() and iteratively fix every error and warning until zero remain. NOTEs are acceptable for this phase.

```r
devtools::check()
```

**Expected errors/warnings to resolve (from research):**

1. "no visible binding for global variable 'X'" — Fix by ensuring all bare column names in dplyr/tidyr verbs use `.data$X`. If Task 2 Step D missed any, add them now. Check all R/ files, not just params.R.

2. "could not find function '%>%'" — Fix by confirming `@importFrom magrittr %>%` is in globals.R and NAMESPACE contains `importFrom(magrittr,pipe)`. Re-run devtools::document().

3. "'getEigenValues' is not a standard unambiguous name" or similar Rcpp registration issue — Fix by confirming `@useDynLib forestIPM, .registration = TRUE` and `@importFrom Rcpp sourceCpp` are in globals.R. Re-run devtools::document() then devtools::load_all().

4. Missing or incorrect DESCRIPTION field — Fix the specific field flagged by check().

5. Any "Undeclared package in Imports" — Add the package to DESCRIPTION Imports and re-run document().

**Iteration loop:** run check(), read output, fix issues, re-run until check() output contains:
```
0 errors ✓ | 0 warnings ✓ | N notes ✗
```
(Notes about missing documentation are expected and acceptable — they will be addressed in Phase 4.)

**Confirm load and basic function availability:**
```r
devtools::load_all()
exists("mkKernel")     # must be TRUE
exists("getEigenValues")  # must be TRUE
exists("init_pop")     # must be TRUE
```
  </action>
  <verify>
    <automated>cd /Users/wvieira/GitHub/forest-IPM && Rscript -e "results <- devtools::check(quiet=FALSE); err <- results\$errors; warn <- results\$warnings; cat('Errors:', length(err), '\n'); cat('Warnings:', length(warn), '\n'); stopifnot(length(err)==0, length(warn)==0); cat('CHECK PASSED\n')"</automated>
  </verify>
  <done>devtools::check() exits with 0 errors and 0 warnings. Output includes "CHECK PASSED". devtools::load_all() succeeds. mkKernel, init_pop, and getEigenValues are accessible after load_all().</done>
</task>

</tasks>

<verification>
Final validation sequence to confirm Phase 2 success criteria are met:

```r
# 1. Load package
devtools::load_all()

# 2. Confirm exported functions exist
stopifnot(exists("mkKernel"))
stopifnot(exists("init_pop"))
stopifnot(exists("getEigenValues"))
stopifnot(exists("getPars_sp"))

# 3. Confirm BUG-01 fix still holds after package compilation
K <- matrix(c(0.8, 0.1, 0.05, 0.9), nrow = 2)
rcpp_lambda <- max(getEigenValues(K))
base_lambda  <- Re(eigen(K)$values[1])
stopifnot(isTRUE(all.equal(rcpp_lambda, base_lambda, tolerance = 1e-10)))

# 4. Run R CMD check — must be 0 errors, 0 warnings
results <- devtools::check()
stopifnot(length(results$errors) == 0)
stopifnot(length(results$warnings) == 0)

cat("Phase 2 success criteria met.\n")
```
</verification>

<success_criteria>
1. DESCRIPTION file exists with Package: forestIPM, correct Imports (Rcpp, dplyr, tidyr, purrr, stringr, rlang, magrittr, truncnorm) and LinkingTo (Rcpp, RcppEigen)
2. NAMESPACE was generated by devtools::document() and contains useDynLib(forestIPM, .registration = TRUE) and all importFrom declarations
3. R/globals.R exists with @useDynLib, @importFrom Rcpp sourceCpp, and all tidyverse @importFrom declarations
4. devtools::check() exits with 0 errors and 0 warnings
5. devtools::load_all() compiles the C++ eigenvalue solver without errors and makes mkKernel, init_pop, getEigenValues, getPars_sp accessible
</success_criteria>

<output>
After completion, create `.planning/phases/02-package-skeleton-and-bug-fixes/02-02-SUMMARY.md` following the summary template at `@/Users/wvieira/.claude/get-shit-done/templates/summary.md`.
</output>
