---
phase: 07-add-github-actions-ci-with-code-coverage-and-pkgdown-site-with-readme-pointing-to-book-vignettes
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - _pkgdown.yml
  - .github/workflows/pkgdown.yaml
autonomous: true
requirements:
  - DOC-04

must_haves:
  truths:
    - "pkgdown site builds locally without errors using Bootstrap 5 materia theme"
    - "pkgdown site deploys automatically to GitHub Pages gh-pages branch on every push to main"
    - "pkgdown navbar includes a 'Book' link pointing to the book documentation site"
    - "pkgdown reference section groups all 11 exported functions into IPM Constructors, IPM Engines, and Utilities"
  artifacts:
    - path: "_pkgdown.yml"
      provides: "pkgdown site configuration with Bootstrap 5, bootswatch materia, book navbar, reference grouping"
      contains: "bootswatch: materia"
    - path: ".github/workflows/pkgdown.yaml"
      provides: "pkgdown CI build + gh-pages deploy workflow"
      contains: "JamesIves/github-pages-deploy-action"
  key_links:
    - from: "_pkgdown.yml"
      to: "https://willvieira.github.io/forest-IPM/"
      via: "url: field"
      pattern: "url:.*willvieira.github.io/forest-IPM"
    - from: ".github/workflows/pkgdown.yaml"
      to: "gh-pages branch"
      via: "JamesIves/github-pages-deploy-action@v4.5.0"
      pattern: "github-pages-deploy-action"
---

<objective>
Create the pkgdown site configuration (`_pkgdown.yml`) and the GitHub Actions workflow that builds and deploys the site to GitHub Pages.

Purpose: Give the package a polished, discoverable documentation site at https://willvieira.github.io/forest-IPM/ — with function reference grouped by purpose and a prominent link to the book documentation.
Output: `_pkgdown.yml` and `.github/workflows/pkgdown.yaml`
</objective>

<execution_context>
@/Users/wvieira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wvieira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-add-github-actions-ci-with-code-coverage-and-pkgdown-site-with-readme-pointing-to-book-vignettes/07-CONTEXT.md
@.planning/phases/07-add-github-actions-ci-with-code-coverage-and-pkgdown-site-with-readme-pointing-to-book-vignettes/07-RESEARCH.md

<interfaces>
<!-- All exported functions from NAMESPACE (as of Phase 6 completion): -->
<!-- Constructors: stand, species_model, env_condition, parameters, control -->
<!-- Engines: lambda, project -->
<!-- Utilities: supported_species, scale_env, unscale_env, set_random_effects -->
<!-- S3 methods (not exported as standalone functions): print.*, summary.*, plot.ipm_projection -->

<!-- Book URL (locked): https://willvieira.github.io/book_forest-demography-IPM/ -->
<!-- pkgdown site URL (locked): https://willvieira.github.io/forest-IPM/ -->
<!-- Theme (locked): bootstrap 5, bootswatch materia -->
<!-- Home page: README.md (no separate index.md) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _pkgdown.yml</name>
  <files>_pkgdown.yml</files>
  <action>
Write `_pkgdown.yml` at the repository root.

Locked decisions from CONTEXT.md:
- `url: https://willvieira.github.io/forest-IPM/` — must be first key (required for absolute URLs and canonical link tag)
- Bootstrap 5 + Bootswatch materia theme
- README.md as home page (do NOT create index.md — pkgdown reads README.md natively when no index.md exists)
- Navbar: Book link pointing to https://willvieira.github.io/book_forest-demography-IPM/
- No vignettes section (book is the documentation)

Reference grouping (derived from NAMESPACE exports):
- "IPM Constructors": stand, species_model, env_condition, parameters, control
- "IPM Engines": lambda, project
- "Utilities": supported_species, scale_env, unscale_env, set_random_effects

Final YAML:

```yaml
url: https://willvieira.github.io/forest-IPM/

template:
  bootstrap: 5
  bootswatch: materia

navbar:
  structure:
    left:  [intro, reference]
    right: [book, github]
  components:
    book:
      text: "Book"
      href: https://willvieira.github.io/book_forest-demography-IPM/

reference:
  - title: "IPM Constructors"
    desc: "Functions to build IPM components"
    contents:
      - stand
      - species_model
      - env_condition
      - parameters
      - control
  - title: "IPM Engines"
    desc: "Functions to run IPM calculations"
    contents:
      - lambda
      - project
  - title: "Utilities"
    desc: "Helper functions"
    contents:
      - supported_species
      - scale_env
      - unscale_env
      - set_random_effects
```

Note on `github` navbar component: pkgdown reads the GitHub repo URL from DESCRIPTION's `URL` field. The DESCRIPTION `URL` field is added in Plan 03 (Task 1). If pkgdown is built before Plan 03 completes, the `github` icon in the navbar may be silently dropped — this is acceptable and resolves automatically when Plan 03 runs.
  </action>
  <verify>
    <automated>Rscript -e "pkgdown::build_site(preview = FALSE)" 2>&1 | tail -5</automated>
  </verify>
  <done>`_pkgdown.yml` exists at repo root with url field, bs5 materia theme, book navbar component, and reference grouping for all 11 exported functions. `pkgdown::build_site()` completes without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create pkgdown.yaml GitHub Actions workflow</name>
  <files>.github/workflows/pkgdown.yaml</files>
  <action>
Write `.github/workflows/pkgdown.yaml`.

Base this on the r-lib/actions pkgdown template (https://github.com/r-lib/actions/blob/v2/examples/pkgdown.yaml). This workflow:
- Builds the pkgdown site on all triggers (push, PR, release, workflow_dispatch)
- Deploys to `gh-pages` branch ONLY on push to main (not on PRs)
- Uses `JamesIves/github-pages-deploy-action@v4.5.0` — do not switch to GitHub's native deploy-pages action

Final YAML:

```yaml
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  release:
    types: [published]
  workflow_dispatch:

name: pkgdown.yaml

permissions: read-all

concurrency:
  group: pkgdown-${{ github.event_name != 'pull_request' || github.run_id }}
  cancel-in-progress: true

jobs:
  pkgdown:
    runs-on: ubuntu-latest
    concurrency:
      group: pkgdown-${{ github.ref }}
      cancel-in-progress: true
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: r-lib/actions/setup-pandoc@v2
      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true
      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::pkgdown, local::.
          needs: website
      - name: Build site
        run: pkgdown::build_site_github_pages(new_process = FALSE, install = FALSE)
        shell: Rscript {0}
      - name: Deploy to GitHub pages
        if: github.event_name != 'pull_request'
        uses: JamesIves/github-pages-deploy-action@v4.5.0
        with:
          clean: false
          branch: gh-pages
          folder: docs
```

Critical post-deployment note for executor to surface in SUMMARY:
GitHub Pages must be manually enabled in repository Settings > Pages > Source = "Deploy from a branch" > Branch = `gh-pages` / `/(root)`. The workflow cannot do this automatically. The site will return 404 until this is done even if the workflow runs green.
  </action>
  <verify>
    <automated>test -f /Users/wvieira/GitHub/forest-IPM/.github/workflows/pkgdown.yaml && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <done>`.github/workflows/pkgdown.yaml` exists with JamesIves deploy step, deploy conditional on non-PR events, and `gh-pages` branch target.</done>
</task>

</tasks>

<verification>
Both files exist and pkgdown builds locally:
- `test -f _pkgdown.yml`
- `test -f .github/workflows/pkgdown.yaml`
- `Rscript -e "pkgdown::build_site(preview = FALSE)"` — exits 0, no errors in output
- `Rscript -e "devtools::test()"` — all tests still pass
</verification>

<success_criteria>
- `_pkgdown.yml` exists with url, bs5 materia theme, book navbar, and all 11 exported functions in reference section
- `.github/workflows/pkgdown.yaml` deploys to gh-pages on push to main only
- `pkgdown::build_site()` completes without errors locally
- Existing testthat suite unaffected: `devtools::test()` green
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-github-actions-ci-with-code-coverage-and-pkgdown-site-with-readme-pointing-to-book-vignettes/07-02-SUMMARY.md`
</output>
