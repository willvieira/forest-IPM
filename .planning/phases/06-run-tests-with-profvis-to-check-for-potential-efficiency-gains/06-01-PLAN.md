---
phase: 06-run-tests-with-profvis-to-check-for-potential-efficiency-gains
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/testthat/test-regression-baselines.R
  - tests/testthat/fixtures/lambda_baseline.rds
  - tests/testthat/fixtures/project_baseline.rds
  - R/BasalArea_competition.R
  - R/control.R
  - R/env_condition.R
  - R/env_scaling.R
  - R/globals.R
  - R/kernel.R
  - R/lambda.R
  - R/parameters.R
  - R/project.R
  - R/species_model.R
  - R/stand.R
  - R/supported_species.R
  - R/vital_rates.R
autonomous: true
requirements: []

must_haves:
  truths:
    - "Baseline QUERUB lambda and project() trajectory are saved to fixtures before any optimization"
    - "All R/ source files use native pipe |> instead of %>%"
    - "sapply/vapply calls replaced with purrr::map_dbl/map_chr/map_lgl equivalents"
    - "All dplyr column references use .data$ pronoun"
    - "R CMD check passes with zero errors and zero warnings after revamp"
    - "testthat suite passes after revamp"
  artifacts:
    - path: "tests/testthat/test-regression-baselines.R"
      provides: "Regression assertions for lambda and project() output against pre-optimization baseline"
      contains: "expect_equal.*tolerance.*1e-10"
    - path: "tests/testthat/fixtures/lambda_baseline.rds"
      provides: "Saved numeric lambda value for QUERUB before any optimization"
    - path: "tests/testthat/fixtures/project_baseline.rds"
      provides: "Saved project() lambda trajectory for QUERUB before any optimization"
  key_links:
    - from: "tests/testthat/test-regression-baselines.R"
      to: "tests/testthat/fixtures/lambda_baseline.rds"
      via: "readRDS"
      pattern: "readRDS.*lambda_baseline"
    - from: "R/globals.R"
      to: "R/ source files"
      via: "@importFrom declarations"
      pattern: "@importFrom purrr map_dbl"
---

<objective>
Capture pre-optimization baselines for QUERUB lambda and project() trajectory, then apply a full tidyverse-style revamp to all R/ source files.

Purpose: The baseline fixtures establish regression anchors — every subsequent optimization must produce numerically identical output (tolerance 1e-10). The tidyverse revamp improves consistency and readability across the package.
Output: Baseline RDS fixtures in tests/testthat/fixtures/, regression test file, and all R/ source files updated to idiomatic tidyverse style with R CMD check passing at zero errors and zero warnings.
</objective>

<execution_context>
@/Users/wvieira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wvieira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/06-CONTEXT.md
@.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/06-RESEARCH.md

<interfaces>
<!-- Key contracts for this plan. Executor: no codebase exploration needed — use these directly. -->

From R/kernel.R:
```r
# Internal functions (not exported) — style revamp changes internals only
vonBertalanffy_lk = function(pars, delta_time, size_t1, size_t0, BA_comp_intra, BA_comp_inter, Temp, Prec, plot_random)
P_xEC = function(size_t1, size_t0, delta_time, BA_comp_intra, BA_comp_inter, Temp, Prec, parsGrowth, parsMort, plot_random)
ingrowth_lk <- function(size_t1, size_t0, delta_time, plot_size, BA_adult_sp, BA_adult, Temp, Prec, parsIngrowth, parsSizeIngrowth, plot_random)
mkKernel = function(Nvec_intra, Nvec_inter, delta_time, plotSize, Temp, Prec, pars, plot_random)
```

From R/BasalArea_competition.R:
```r
# sapply -> purrr::map_dbl target
sapply(N_intra$meshpts, function(x) sum(ba_N_plot[N_focal$meshpts > x]))
```

From R/globals.R — current @importFrom declarations:
```r
#' @importFrom purrr map map_chr
#' @importFrom dplyr filter group_by reframe select mutate case_match group_split
#' @importFrom magrittr %>%
```

From R/project.R — vapply pattern to update:
```r
vapply(mod$species, function(sp) { is.null(pars$species_params[[sp]]$fixed) }, logical(1))
```

Public API (DO NOT change signatures):
```r
# Exported functions — signatures locked from Phase 1
mkKernel(Nvec_intra, Nvec_inter, delta_time, plotSize, Temp, Prec, pars, plot_random)
project(mod, pars, stand, env, ctrl)
lambda(mod, pars, stand, env, ctrl)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture pre-optimization baselines and write regression test</name>
  <files>tests/testthat/fixtures/lambda_baseline.rds, tests/testthat/fixtures/project_baseline.rds, tests/testthat/test-regression-baselines.R</files>
  <behavior>
    - Test 1: lambda(QUERUB_mod, QUERUB_pars, QUERUB_stand, env, ctrl) returns a numeric scalar equal to lambda_baseline.rds within tolerance 1e-10
    - Test 2: project(QUERUB_mod, QUERUB_pars, QUERUB_stand, env, ctrl) final lambda matches last value in project_baseline.rds within tolerance 1e-10
    - Note: fixtures must be created FIRST (saveRDS), then tests assert equality — RED phase will fail until fixtures exist; GREEN after fixtures are present
  </behavior>
  <action>
    Step 1 — Create the fixtures directory if it doesn't exist: `dir.create("tests/testthat/fixtures", recursive = TRUE, showWarnings = FALSE)`

    Step 2 — Generate baseline fixtures. Run this R snippet (in devtools::load_all() context) to build QUERUB inputs and capture outputs:

    ```r
    devtools::load_all()
    # Minimal synthetic QUERUB stand — sizes spaced across realistic DBH range
    df_single <- data.frame(
      size_mm    = seq(130, 600, by = 50),
      species_id = "QUERUB",
      plot_size  = 1000
    )
    s    <- stand(df_single)
    mod  <- species_model(s)
    pars <- parameters(mod, draw = "mean")
    env  <- env_condition(MAT = 10, MAP = 1100)
    ctrl_project <- control(years = 10, compute_lambda = TRUE)

    lambda_baseline <- lambda(mod, pars, s, env)
    project_out     <- project(mod, pars, s, env, ctrl_project)
    project_baseline <- project_out$lambda[["QUERUB"]]

    saveRDS(lambda_baseline, "tests/testthat/fixtures/lambda_baseline.rds")
    saveRDS(project_baseline, "tests/testthat/fixtures/project_baseline.rds")
    ```

    Step 3 — Write test-regression-baselines.R:

    ```r
    # tests/testthat/test-regression-baselines.R
    # Regression guard: any optimization must produce output identical to this baseline.
    # Tolerance 1e-10 catches floating-point drift from refactoring; must fail for wrong math.

    test_that("lambda() output matches pre-optimization baseline for QUERUB", {
      skip_if_not(file.exists(testthat::test_path("fixtures/lambda_baseline.rds")),
                  "Baseline fixture not found — run Task 1 setup to generate it")

      df_single <- data.frame(
        size_mm    = seq(130, 600, by = 50),
        species_id = "QUERUB",
        plot_size  = 1000
      )
      s    <- stand(df_single)
      mod  <- species_model(s, species = "QUERUB")
      pars <- parameters(mod, draw = "mean")
      env  <- env_condition(MAT = 10, MAP = 1100)
      ctrl <- control(years = 1)

      result   <- lambda(mod, pars, s, env, ctrl)
      baseline <- readRDS(testthat::test_path("fixtures/lambda_baseline.rds"))

      expect_equal(result, baseline, tolerance = 1e-10)
    })

    test_that("project() lambda trajectory matches pre-optimization baseline for QUERUB", {
      skip_if_not(file.exists(testthat::test_path("fixtures/project_baseline.rds")),
                  "Baseline fixture not found — run Task 1 setup to generate it")

      df_single <- data.frame(
        size_mm    = seq(130, 600, by = 50),
        species_id = "QUERUB",
        plot_size  = 1000
      )
      s    <- stand(df_single)
      mod  <- species_model(s, species = "QUERUB")
      pars <- parameters(mod, draw = "mean")
      env  <- env_condition(MAT = 10, MAP = 1100)
      ctrl <- control(years = 10, compute_lambda = TRUE)

      result   <- project(mod, pars, s, env, ctrl)$lambda[["QUERUB"]]
      baseline <- readRDS(testthat::test_path("fixtures/project_baseline.rds"))

      expect_equal(result, baseline, tolerance = 1e-10)
    })
    ```

    Step 4 — Run `devtools::test(filter = "regression")` to confirm tests pass with the saved fixtures.
  </action>
  <verify>
    <automated>Rscript -e "devtools::load_all(); devtools::test(filter = 'regression')"</automated>
  </verify>
  <done>Both regression tests pass. fixtures/lambda_baseline.rds and fixtures/project_baseline.rds exist and are non-empty RDS files.</done>
</task>

<task type="auto">
  <name>Task 2: Tidyverse-style revamp of all R/ source files</name>
  <files>R/BasalArea_competition.R, R/control.R, R/env_condition.R, R/env_scaling.R, R/globals.R, R/kernel.R, R/lambda.R, R/parameters.R, R/project.R, R/species_model.R, R/stand.R, R/supported_species.R, R/vital_rates.R</files>
  <action>
    Perform a full tidyverse-style pass over all R/ source files (excluding RcppExports.R — auto-generated, do not touch).

    **Rules (apply to every file):**

    1. Replace `%>%` with `|>` (native pipe). Remove `@importFrom magrittr %>%` from globals.R after all usages are gone.

    2. Replace `sapply()` with `purrr::map_dbl()` / `purrr::map_chr()` / `purrr::map_lgl()` depending on output type. Replace `vapply(x, f, logical(1))` with `purrr::map_lgl(x, f)` and `vapply(x, f, numeric(1))` with `purrr::map_dbl(x, f)`.
       - Exception: Do NOT replace the `sapply()` call inside `size_to_BAcomp()` in BasalArea_competition.R with a purrr equivalent IF it creates a readability regression (this call is O(n) at function level, not inside a hot loop — style improvement only).

    3. Ensure all `dplyr::filter()`, `dplyr::select()`, `dplyr::mutate()` calls reference columns via `.data$colname`, not bare names. This prevents R CMD check NOTEs.

    4. Replace `purrr::map_dfr()` / `purrr::map_dfc()` (deprecated in purrr 1.0.0) with `purrr::map() |> purrr::list_rbind()` / `purrr::list_cbind()` if any exist.

    5. Do NOT change function signatures or return types. The public API is locked from Phase 1. Style revamp changes internals only.

    6. Do NOT touch `R/RcppExports.R` — auto-generated by Rcpp::compileAttributes().
    
    7. Prefer pipe continous workflow over the line-by-line R baseline workflow.

    **globals.R updates:** After the revamp, add any new purrr/dplyr symbols introduced (e.g., `map_dbl`, `map_lgl`, `map_chr`, `list_rbind`) to the `@importFrom purrr` and `@importFrom dplyr` lines. Remove `@importFrom magrittr %>%` once all `%>%` usages are eliminated. Run `devtools::document()` after updating globals.R (use `roxygen2::roxygenise(load_code = 'source')` if C++ compilation is blocked by Xcode license issue — see STATE.md blocker).

    **File-by-file priorities:**
    - `R/BasalArea_competition.R`: `sapply()` -> `purrr::map_dbl()`
    - `R/project.R`: `vapply()` -> `purrr::map_lgl()`, any `%>%` -> `|>`
    - `R/lambda.R`: check for `vapply()` -> `purrr::map_dbl()`
    - `R/parameters.R`: already partially tidyverse; confirm `.data$` pronouns, replace any `%>%`
    - All others: pipe replacement + `.data$` audit

    After all file edits are complete, run `devtools::check()` and address any NOTEs about undeclared symbols by adding them to `@importFrom` in globals.R.
  </action>
  <verify>
    <automated>Rscript -e "devtools::check()" 2>&1 | tail -20</automated>
  </verify>
  <done>devtools::check() produces zero errors and zero warnings. devtools::test() passes all existing tests including the regression baseline tests from Task 1. No `%>%` appears in any R/ file. No `sapply`/`vapply` in non-hot code paths.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `devtools::test()` — all tests pass including regression baselines
2. `devtools::check()` — zero errors, zero warnings
3. `grep -r "%>%" R/` returns no matches
4. `ls tests/testthat/fixtures/` shows lambda_baseline.rds and project_baseline.rds
</verification>

<success_criteria>
- Pre-optimization baselines exist as RDS fixtures and regression tests pass against them
- All R/ source files use native pipe |> instead of magrittr %>%
- sapply/vapply replaced with typed purrr equivalents throughout
- All dplyr column references use .data$ pronoun
- devtools::check() zero errors, zero warnings
- testthat suite fully green
</success_criteria>

<output>
After completion, create `.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/06-01-SUMMARY.md`
</output>
