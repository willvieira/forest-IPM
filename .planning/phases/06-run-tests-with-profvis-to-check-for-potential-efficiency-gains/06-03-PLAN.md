---
phase: 06-run-tests-with-profvis-to-check-for-potential-efficiency-gains
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - R/kernel.R
  - R/project.R
  - src/kernel_ops.cpp
  - R/RcppExports.R
  - .planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/profile_benchmark.R
  - .planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/PROFILING.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "Only optimizations approved by human in Plan 02 checkpoint are applied"
    - "lambda() and project() produce output numerically identical to pre-optimization baselines (tolerance 1e-10)"
    - "R CMD check passes with zero errors and zero warnings after all optimizations"
    - "Before/after benchmark comparison documents measurable speedup for each applied optimization"
    - "PROFILING.md is updated with final benchmark comparison results"
  artifacts:
    - path: "R/kernel.R"
      provides: "mkKernel() with vectorized outer() replacement (if Target 1 approved)"
    - path: "R/project.R"
      provides: "project() with BA competition metric caching (if Target 2 approved)"
    - path: ".planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/PROFILING.md"
      provides: "Updated with before/after benchmark results"
      contains: "speedup|improvement|% faster"
  key_links:
    - from: "R/kernel.R (vectorized outer)"
      to: "tests/testthat/fixtures/lambda_baseline.rds"
      via: "regression test assertion"
      pattern: "expect_equal.*tolerance.*1e-10"
    - from: "src/kernel_ops.cpp (if Target 3 applied)"
      to: "R/RcppExports.R"
      via: "Rcpp::compileAttributes()"
      pattern: "Rcpp::export"
---

<objective>
Apply the optimizations approved in Plan 02, verify correctness against regression baselines, and document the measurable speedup for each optimization applied.

Purpose: Convert profiling evidence into actual performance gains while ensuring correctness — no optimization ships without passing the regression baseline test.
Output: Optimized R/kernel.R and/or R/project.R (and optionally src/kernel_ops.cpp), an updated PROFILING.md with before/after benchmark comparison, and a passing R CMD check.
</objective>

<execution_context>
@/Users/wvieira/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wvieira/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/06-CONTEXT.md
@.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/06-RESEARCH.md
@.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/PROFILING.md
@.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/06-02-SUMMARY.md

<interfaces>
<!-- Current mkKernel() outer() calls — the primary optimization target. -->

From R/kernel.R (current implementation):
```r
# P matrix: n^2 R function calls via outer()
P <- h * outer(
  meshpts, meshpts,
  P_xEC,
  delta_time, BA_comp_intra, BA_comp_inter, Temp, Prec,
  pars[['growth']], pars[['mort']], plot_random
)

# F matrix: n^2 R function calls via outer()
F <- h * outer(
  meshpts, meshpts,
  ingrowth_lk,
  delta_time, plotSize, BAplot_intra, BAplot_total, Temp, Prec,
  pars[['rec']], pars[['sizeIngrowth']], plot_random[3]
)
```

Vectorized replacement pattern (from RESEARCH.md):
```r
# outer(X, Y, FUN) calls FUN(X_expanded, Y_expanded)
# outer's X -> FUN's 1st arg (size_t1); outer's Y -> FUN's 2nd arg (size_t0)
n   <- length(meshpts)
s1  <- rep(meshpts, each = n)    # outer's X -> P_xEC's size_t1 (1st arg)
s0  <- rep(meshpts, times = n)   # outer's Y -> P_xEC's size_t0 (2nd arg)
P_vals <- P_xEC(s1, s0, delta_time, BA_comp_intra, BA_comp_inter, Temp, Prec,
                pars[['growth']], pars[['mort']], plot_random)
P   <- h * matrix(P_vals, nrow = n, ncol = n)
```

CRITICAL: Verify argument order matches outer() semantics before replacing.
Add unit test on a 3x3 kernel to catch transposition bugs.

For Target 2 (BA caching) — project() loop structure:
```r
for (t in seq_len(ctrl$years)) {
  for (sp in mod$species) {
    K_list <- mkKernel(Nvec_intra = nvec_list[[sp]]$N_con,
                       Nvec_inter  = nvec_list[[sp]]$N_het, ...)
    # BA_comp_intra and BAplot_intra depend on nvec_list[[sp]]$N_con$Nvec
    # BA_comp_inter and BAplot_inter depend on nvec_list[[sp]]$N_het$Nvec
    # N_het is updated each timestep via .update_N_het() — DO NOT CACHE inter metrics
  }
}
```

For Target 3 (C++ migration) — template from existing src/eigen.cpp:
```cpp
#include <RcppEigen.h>
// [[Rcpp::depends(RcppEigen)]]
// [[Rcpp::export]]
```
Run Rcpp::compileAttributes() after adding any new [[Rcpp::export]] function.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply approved optimizations to kernel.R and/or project.R</name>
  <files>R/kernel.R, R/project.R, src/kernel_ops.cpp</files>
  <behavior>
    - Test 1 (regression): lambda(QUERUB) == lambda_baseline (tolerance 1e-10) — MUST pass after every optimization
    - Test 2 (regression): project(QUERUB, 10 years) lambda trajectory == project_baseline (tolerance 1e-10) — MUST pass after every optimization
    - Test 3 (if Target 1 applied): 3x3 micro-kernel built with vectorized method == 3x3 kernel built with original outer() — element-wise equality, no tolerance (exact match verifies no transposition)
  </behavior>
  <action>
    Read PROFILING.md and the human decision from the Plan 02 checkpoint to determine which targets are approved. Apply ONLY the approved optimizations.

    **Target 1: Vectorize outer() calls in mkKernel() (R/kernel.R)**

    If approved: Replace both `outer()` calls in `mkKernel()` with the vectorized `rep()` + direct evaluation + `matrix()` pattern.

    Before replacing, add a 3x3 correctness test:
    ```r
    # Verification that vectorized == outer — run interactively before replacing
    test_meshpts <- c(130, 200, 300)
    n_test <- length(test_meshpts)
    # ... build tiny P matrix both ways and check all.equal(P_outer, P_vec)
    ```

    Replacement for P matrix:
    ```r
    n   <- length(meshpts)
    s1  <- rep(meshpts, each  = n)   # outer's X -> P_xEC's 1st arg (size_t1)
    s0  <- rep(meshpts, times = n)   # outer's Y -> P_xEC's 2nd arg (size_t0)
    P_vals <- P_xEC(
      s1, s0,
      delta_time, BA_comp_intra, BA_comp_inter, Temp, Prec,
      pars[['growth']], pars[['mort']], plot_random
    )
    P <- h * matrix(P_vals, nrow = n, ncol = n)
    ```

    Replacement for F matrix (ingrowth_lk):
    ```r
    # ingrowth_lk: 1st arg = size_t1, 2nd arg = size_t0 (same convention)
    F_vals <- ingrowth_lk(
      s1, s0,
      delta_time, plotSize, BAplot_intra, BAplot_total, Temp, Prec,
      pars[['rec']], pars[['sizeIngrowth']], plot_random[3]
    )
    F <- h * matrix(F_vals, nrow = n, ncol = n)
    ```

    After replacement: run `devtools::test(filter = "regression")` — MUST pass. If lambda changes by more than 1e-10, check argument order (each= vs times= swap).

    **Target 2: Cache competition metrics in project() loop (R/project.R + R/kernel.R)**

    If approved: Add optional `BA_comp_intra`, `BAplot_intra` arguments to `mkKernel()`. Compute them once before the timestep loop and pass in for single-species runs. For multi-species, only cache intraspecific metrics (BA_comp_intra, BAplot_intra) per species — never cache inter-species metrics (BA_comp_inter, BAplot_inter change every timestep).

    Change mkKernel() signature:
    ```r
    mkKernel = function(
      Nvec_intra, Nvec_inter,
      delta_time, plotSize, Temp, Prec, pars,
      plot_random,
      BA_comp_intra = NULL,  # optional cache
      BAplot_intra  = NULL   # optional cache
    ){
      # Only compute if not provided
      if (is.null(BA_comp_intra)) {
        BA_comp_intra <- size_to_BAcomp(N_intra = Nvec_intra, plot_size = plotSize)
      }
      if (is.null(BAplot_intra)) {
        BAplot_intra <- size_to_BAplot(N = Nvec_intra, plot_size = plotSize)
      }
      # BA_comp_inter and BAplot_inter always recomputed (Nvec_inter changes every t)
      ...
    }
    ```

    RISK: Do NOT cache BA_comp_inter — Nvec_inter (the competitor's distribution) is updated each timestep via .update_N_het(). Caching inter metrics would use stale competition values and break multi-species correctness.

    After change: run `devtools::test(filter = "regression")` — MUST pass for both single and multi-species configurations.

    **Target 3: C++ migration of P_xEC (src/kernel_ops.cpp)**

    If approved: Create `src/kernel_ops.cpp` following the pattern from `src/eigen.cpp`. Implement a C++ function that takes meshpts, parameters, and environmental inputs and returns the full P matrix directly. Use Eigen::MatrixXd.

    After creating the file:
    1. Run `Rcpp::compileAttributes()` — regenerates R/RcppExports.R
    2. Run `devtools::load_all()` — compiles and loads the new function
    3. Update `mkKernel()` in R/kernel.R to call the C++ function instead of the vectorized R version
    4. Run `devtools::test(filter = "regression")` — MUST pass

    NOTE: If Xcode license is not accepted (STATE.md blocker), C++ compilation will fail. In that case, skip Target 3 and document in PROFILING.md why it was skipped.

    **After all approved optimizations:**
    1. Run `devtools::test()` — full testthat suite must pass
    2. Run `devtools::check()` — zero errors, zero warnings
  </action>
  <verify>
    <automated>Rscript -e "devtools::load_all(); devtools::test(filter = 'regression')" 2>&1 | tail -10</automated>
  </verify>
  <done>All regression tests pass (lambda and project() trajectory match baselines to 1e-10). devtools::check() produces zero errors, zero warnings. Each applied optimization has a passing regression test confirming mathematical correctness.</done>
</task>

<task type="auto">
  <name>Task 2: Run before/after benchmark and update PROFILING.md</name>
  <files>.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/profile_benchmark.R, .planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/PROFILING.md</files>
  <action>
    Add a before/after benchmark section to profile_benchmark.R and run it to quantify the speedup from each applied optimization.

    Append to profile_benchmark.R:
    ```r
    # ── Before/after benchmark ────────────────────────────────────────────────────
    # Re-run after optimizations are applied.
    # "original" baseline was captured in the initial microbenchmark (mb_kernel above).
    # This section measures the optimized mkKernel() using the same inputs.

    mb_optimized <- microbenchmark(
      mkKernel_optimized = {
        mkKernel(
          Nvec_intra  = nvec_list[["QUERUB"]]$N_con,
          Nvec_inter  = nvec_list[["QUERUB"]]$N_het,
          delta_time  = ctrl_1$delta_time,
          plotSize    = s_single$plot_size,
          Temp        = env$.MAT_scl,
          Prec        = env$.MAP_scl,
          pars        = sp_pars,
          plot_random = c(0, 0, 0)
        )
      },
      times = 50
    )
    print(mb_optimized)

    # Compare original vs optimized
    library(bench)
    bench_comparison <- bench::mark(
      original  = {
        # ... same mkKernel() call
      },
      optimized = {
        # ... same mkKernel() call (now runs the optimized code path)
      },
      iterations = 50,
      check = FALSE  # outputs differ only if math changed — regression tests already confirm
    )
    print(bench_comparison)
    ```

    Run the updated script:
    ```r
    devtools::load_all()
    source(".planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/profile_benchmark.R")
    ```

    Update PROFILING.md with a "Before/After Results" section:
    ```markdown
    ## Before/After Benchmark Results

    ### Target 1: Vectorize outer() — mkKernel() single call (50 reps, microbenchmark)

    | Metric | Before | After | Speedup |
    |--------|--------|-------|---------|
    | Median | {X} ms | {Y} ms | {X/Y}x faster |
    | IQR    | {lo-hi} ms | {lo-hi} ms | — |
    | Total (100 steps) | ~{X*100} ms | ~{Y*100} ms | ~{savings} ms saved |

    ### Target 2: BA competition caching (if applied)
    | Metric | Before | After | Speedup |
    |--------|--------|-------|---------|
    | (fill from bench_comparison output) |

    ### Target 3: C++ P_xEC migration (if applied)
    | Metric | Before | After | Speedup |
    |--------|--------|-------|---------|
    | (fill from bench_comparison output) |

    ### Summary

    {2-3 sentences: which optimizations were applied, total measured speedup, whether the >10-15% threshold was met, anything evaluated but skipped and why.}
    ```

    Fill in all {placeholders} with real numeric values from the benchmark run. If an optimization was evaluated but skipped (e.g., < 10% gain, or Xcode license blocked C++), document it explicitly.
  </action>
  <verify>
    <automated>Rscript -e "md <- readLines('.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/PROFILING.md'); stopifnot(!any(grepl('\\\\{[A-Za-z]', md))); cat('No unfilled placeholders in PROFILING.md\n')"</automated>
  </verify>
  <done>PROFILING.md updated with real before/after numeric data. No {placeholder} strings remain. Summary section documents each optimization: applied or skipped and why.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify optimization correctness and benchmark results</name>
  <action>Human runs regression tests and reviews before/after benchmark numbers in PROFILING.md to confirm all optimizations are correct and speedup is as expected.</action>
  <what-built>
    Applied the optimizations approved in Plan 02. Regression tests confirm lambda() and project() output is numerically identical to pre-optimization baselines (tolerance 1e-10). PROFILING.md updated with before/after benchmark numbers for each optimization. devtools::check() passes with zero errors and zero warnings.
  </what-built>
  <how-to-verify>
    1. Run `devtools::test(filter = "regression")` — confirm both regression tests pass.
    2. Run `devtools::check()` — confirm zero errors, zero warnings.
    3. Read the "Before/After Benchmark Results" section in PROFILING.md — confirm the reported speedups match your expectations from the Plan 02 flamegraph review.
    4. If C++ migration was applied: verify `R/RcppExports.R` was regenerated (should contain the new function signature) and `src/kernel_ops.cpp` exists.
  </how-to-verify>
  <resume-signal>Type "approved" if the results look correct, or describe any issues (wrong speedup numbers, unexpected test failures, optimizations that should be reverted).</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
1. `devtools::test(filter = "regression")` — both baseline regression tests pass
2. `devtools::test()` — full testthat suite passes
3. `devtools::check()` — zero errors, zero warnings
4. PROFILING.md contains actual before/after benchmark numbers with no {placeholders}
5. Human confirms speedup results and optimization correctness
</verification>

<success_criteria>
- All approved optimizations applied to R/kernel.R and/or R/project.R (and src/ if C++ migration)
- Regression test for lambda() passes: output == pre-optimization baseline to 1e-10
- Regression test for project() lambda trajectory passes: output == pre-optimization baseline to 1e-10
- devtools::check() zero errors, zero warnings
- PROFILING.md before/after section documents measurable speedup (or explicit "no gain" with reason)
- Human has verified results and approved the phase as complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-run-tests-with-profvis-to-check-for-potential-efficiency-gains/06-03-SUMMARY.md`
</output>
